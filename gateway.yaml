server:
   port: 8079
   
spring:
   application.name: gateway
   cloud:
     gateway:
       default-filters:
       - PrefixPath=/
       routes:
       #======================
       - id: user
         uri: lb://user
         predicates:
         - Path=/user/**
         filters:
         - StripPrefix=1
       #======================
